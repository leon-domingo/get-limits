# Small binary which returns the limits a battery should be charged and discharged for the current day

Usage is very simple:

```shell
get-limits [both|top|bottom]
```

For example:

```shell
get-limits both
```
